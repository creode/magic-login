{% if craft.app.session.hasFlash('notice') %}
    <p class="message notice">{{ craft.app.session.getFlash('notice') }}</p>
{% elseif craft.app.session.hasFlash('error') %}
    <p class="message error">{{ craft.app.session.getFlash('error') }}</p>
{% endif %}

<form method="post" id="magic-login-form">
    {{ actionInput('magic-login/magic-login/login') }}
    {{ csrfInput() }}

    <label for="email">Email</label>
    <input id="email" name="email" value="" placeholder="{{ 'Enter a valid email...'|t('magic-login') }}" />

    <button type="submit" name="submitButton">{{ 'Submit'|t('magic-login') }}</button>
</form>