<html>
    <head>
    </head>
    <body>
        {{ beginBody() }}

        <div class="ml-wrap">

            {% if craft.app.session.hasFlash('notice') %}
                <p class="message notice">{{ craft.app.session.getFlash('notice') }}</p>
            {% elseif craft.app.session.hasFlash('error') %}
                <p class="message error">{{ craft.app.session.getFlash('error') }}</p>
            {% endif %}

            <p class="ml-instructions">{{ 'Please enter your email below to continue.'|t('magic-login') }}</p>

            <form method="post" class="ml-form" id="magic-login-form">
                {{ actionInput('magic-login/magic-login/login-or-register') }}
                {{ redirectInput('magic-login/login-link-sent') }}
                {{ csrfInput() }}

                <label for="email">Email</label>
                <input id="email" type="email" name="email" value="" placeholder="{{ 'Enter a valid email...'|t('magic-login') }}" /><br/>

                <button type="submit" class="ml-button" name="submitButton">{{ 'Login or Register'|t('magic-login') }}</button>
            </form>

        </div>

        {% do view.registerAssetBundle("creode\\magiclogin\\assetbundles\\magiclogin\\MagicLoginAsset") %}

        {{ endBody() }}
    </body>
</html>